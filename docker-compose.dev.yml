services:
  website-backend:
    platform: linux/arm64
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
      - ./tsconfig.json:/app/tsconfig.json
      - ./package.json:/app/package.json
      - ./package-lock.json:/app/package-lock.json

      - ./.env:/app/.env
      - ./data:/app/data
      - ./backup:/app/backup
    environment:
      - NODE_ENV=development
    env_file:
      - .env
    command: sh -c "if [ ! -d node_modules ]; then pnpm install; fi && pnpm run dev"
